<!-- SUPERSEDED — do not use. This template is retired (EPIC-09).

     Per-task outcomes are no longer local `.claude/memory/epics/<name>/yyyy-mm-dd-<task>.md` files.
     In the Meaningfy spine the task breakdown lives in the PLAN:

         tasks  ≡  openspec/changes/<id>/tasks.md      (checkbox list the `/opsx:apply` phase tracks)

     Implementation outcomes accrue in the change itself (commits citing the change id) and merge
     into openspec/specs/ on archive. This file is kept only as a redirect; the scaffolder no
     longer renders it into a repo. -->
