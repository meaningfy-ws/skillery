<!-- SUPERSEDED — do not use. This template is retired (EPIC-09).

     The PLAN is no longer a local `.claude/memory/epics/<name>/PLAN.md` file. In the Meaningfy
     spine the PLAN *is* the OpenSpec design + tasks pair:

         PLAN  ≡  openspec/changes/<id>/design.md  +  openspec/changes/<id>/tasks.md

     The clarity gate scores the pair (≥9/10) before `/opsx:apply`. The templates and the planning
     procedure are owned by the spine — see openspec/schemas/meaningfy/templates/design.md and
     tasks.md, and the epic-planning + clarity-gate skills. This file is kept only as a redirect;
     the scaffolder no longer renders it into a repo. -->
