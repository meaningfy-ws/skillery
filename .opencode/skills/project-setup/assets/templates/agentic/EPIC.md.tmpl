<!-- SUPERSEDED — do not use. This template is retired (EPIC-09).

     The EPIC is no longer a local `.claude/memory/epics/<name>/EPIC.md` file. In the Meaningfy
     spine an EPIC *is* the OpenSpec proposal:

         EPIC  ≡  openspec/changes/<id>/proposal.md      (the Shape-Up work shape)

     Author it with `/opsx:propose` (or `/opsx:explore` first). The shape, the template, and the
     clarity gate are owned by the spine — see openspec/schemas/meaningfy/templates/proposal.md and
     the epic-planning skill. This file is kept only as a redirect; the scaffolder no longer
     renders it into a repo. -->
